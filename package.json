{
  "name": "dreamteam",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:admin": "turbo dev --filter=admin",
    "dev:user-web": "turbo dev --filter=user-web",
    "dev:user-mobile": "turbo dev --filter=user-mobile",
    "dev:server": "turbo dev --filter=agent-server",
    "build": "turbo build",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.0.0"
  }
}
